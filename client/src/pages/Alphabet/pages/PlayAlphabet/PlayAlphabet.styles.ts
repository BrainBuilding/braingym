import { styled } from "styled-components";
import { spaces } from "styles";

export const PlayAlphabetStyled = styled.div`
  padding: ${spaces.lg};

  .level-and-points {
    margin-bottom: ${spaces.lg};
  }

  .button-wrapper {
    margin-top: ${spaces.md};
    margin-bottom: ${spaces.md};
  }
`;
